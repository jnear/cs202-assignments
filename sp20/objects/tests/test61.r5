class Point extends Object {
  x: Integer,
  y: Integer

  def add(self: Point, other: Point): Point = {
    Point(p1.x + p2.x, p1.y + p2.y)
  }
}

let p1 = Point(2,3) in
let p2 = Point(5,6) in
let p3 = (p1.add)(p2) in
p3.x
